{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "rm -rf build && cd ./client && ng build --prod && cp -r dist/dashboard ../build"
  },
  "engines": { "node": "16.13.2", "npm": "8.1.2" },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/cli": "^13.2.0",
    "@angular/compiler-cli": "^13.2.0",
    "express": "^4.17.2"
  }
}
