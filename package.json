{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "rm -rf build && cd ./client && npm run build --prod && cp -r dist/dashboard ../build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@angular/cli": "^13.2.0",
    "@angular/compiler-cli": "^13.2.0",
    "express": "^4.17.2"
  }
}
